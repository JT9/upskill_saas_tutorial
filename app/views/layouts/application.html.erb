<!DOCTYPE html>
<html>
  <head>
    <title>Saasapp</title>
    <%= csrf_meta_tags %>
    <link href="//fonts.googleapis.com/css?family=Open+Sans:400,700,800" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "https://js.stripe.com/v2/", type: 'text/javascript' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= tag :meta, :name => "stripe-key", :content => STRIPE_PUBLIC_KEY %>
  </head>

  <body>
    <nav class="navbar navbar-default navbar-static-top" role="navigation">
      <div class='container'>
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-nav-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          
          <!-- <a href="/" class="navbar-brand"><i class="fa fa-group"></i> DevMatch</> -->
          <%= link_to root_path, class: "navbar-brand" do %>
            <i class="fa fa-group"></i>
            DevMatch
          <% end %>
          
        </div>
        <div class="collapse navbar-collapse" id="main-nav-collapse">
          <div class="nav navbar-nav navbar-right">

            <% if user_signed_in? %>
              <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-default navbar-btn" %>
            <% else %>
            <!-- Pure HTML way
            <a href="/users/sign_in" class="btn btn-default navbar-btn">Login</a>
            <a href="/users/sign_up" class="btn btn-default navbar-btn">Sign Up</a>
            -->
              <%= link_to "Log In", new_user_session_path, class: "btn btn-default navbar-btn" %>
              
            <% end %>
          </div>
          <ul class="nav navbar-nav navbar-right">
            
            <!-- <li><a href="/about">About</a></li> -->
            <li><%= link_to "About", about_path %></li>
            <li><%= link_to "Contact Us", new_contact_path %></li>
            
          </ul>
        </div><!-- /.navbar-collapse -->
      </div>
    </nav>
    
    <!-- Placed flash message here as you may redirect to another page instead of in views->contacts->new.html.erb -->
    <div class="container">
      <!-- loop for |key, value| used for bootstrap alerts for success and danger-->
      <% flash.each do |type, msg| %> <!-- This displays a message on the contact form for message sent or an error in a loop-->
        <%= content_tag :div, msg, class: "alert alert-#{type}" %> <!-- equals sign injects into the document -->
        <!-- <div class ="alert alert-type">msg</div> -->
      <% end %>
      
    </div>
    <%= yield %>

  </body>
</html>
